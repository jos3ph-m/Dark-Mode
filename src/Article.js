import React from 'react';
import moment from 'moment';
const Article = ({ title, snippet, date, length }) => {
  return <article className="post">article component</article>;
};

export default Article;
